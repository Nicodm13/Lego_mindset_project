# shared_data.py

# These two lists store grid coordinates selected by the user during interaction
# with the OpenCV editor.

# Coordinates marked as safe (typically green in the UI).
# Each element is a tuple: (grid_x, grid_y)
safe_coordinates = []

# Coordinates marked as unsafe (typically red in the UI).
# Also stored as (grid_x, grid_y)
unsafe_coordinates = []
